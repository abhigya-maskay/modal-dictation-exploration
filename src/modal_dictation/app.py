"""Placeholder application module."""

from __future__ import annotations


def greet(name: str) -> str:
    """Return a friendly greeting, used to verify the toolchain."""
    return f"Hello, {name}."
